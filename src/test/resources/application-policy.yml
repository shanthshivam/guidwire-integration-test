# src/test/resources/application-policy.yml (for Policy Service tests)
spring:
  application:
    name: policy-service
  
  datasource:
    # Will be set by TestContainers - PostgreSQL
    url: jdbc:postgresql://localhost:5432/policy_service
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082